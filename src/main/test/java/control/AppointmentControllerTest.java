package main.test.java.control;

import org.junit.Test;
import static org.junit.Assert.*;
import control.AppointmentController;

public class AppointmentControllerTest {
    @Test
    public void testScheduleAppointment() {
        AppointmentController controller = new AppointmentController();
        // TODO: Implement test for scheduling appointments
        // For now, we'll just assert that the controller is not null
        assertNotNull(controller);
    }

    @Test
    public void testCancelAppointment() {
        AppointmentController controller = new AppointmentController();
        // TODO: Implement test for canceling appointments
        // For now, we'll just assert that the controller is not null
        assertNotNull(controller);
    }
}